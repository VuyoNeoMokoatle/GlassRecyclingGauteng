<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Buyalot.AddProductPage"
              BackgroundColor="White">
            
    <StackLayout Padding="10">
        <Label Text="Available Products" FontSize="Large" HorizontalOptions="Center" />

        <CollectionView ItemsSource="{Binding Products}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border BackgroundColor="#CCCCCC"  Padding="10" Margin="5">
                        <StackLayout>
                            <Image Source="{Binding ImageUrl}" HeightRequest="100" />
                            <Label Text="{Binding Name}" FontSize="Medium" HorizontalOptions="Center" />
                            <Label Text="{Binding Price, StringFormat='R{0}'}" FontSize="Medium" TextColor="#28a745" HorizontalOptions="Center"/>
                            <Button Text="Add to Cart" 
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=AddToCartCommand}" 
                                    CommandParameter="{Binding}" 
                                    BackgroundColor="#007bff" 
                                    TextColor="White" />
                        </StackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

       
    </StackLayout>
</ContentPage>